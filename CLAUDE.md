# Local Provider Docs

This repo will contain a nodejs app that will run a local website showing docs for any Pulumi parameterised provider that is in the program.

Ideally it will be run using npx (as in `npx local-provider-docs`).

It should tanstack start as the framework with react. It should get the schema for any local providers used in the pulumi project.
