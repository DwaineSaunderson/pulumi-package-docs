# Screenshots

Image assets referenced from a pull request description. Nothing here is part
of the project source; this branch exists only so the images have a stable URL.

Captured from a local Pulumi project with the real `pulumi-random` and
`pulumi-tls` schemas.
